services:
  deepwiki:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - data:/home/deepwiki/app/data
      - claude-config:/home/deepwiki/.claude
    environment:
      - GH_TOKEN=${GH_TOKEN:-}
    restart: unless-stopped

volumes:
  data:
  claude-config:
