services:
  deepwiki:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - data:/home/deepwiki/app/data
      - claude-config:/home/deepwiki/.claude
    env_file:
      - .env.docker
    restart: unless-stopped

volumes:
  data:
  claude-config:
